# Codex 기본 작업 지침

## Feature information architecture rules

- Keep feature screens focused on the primary task. Do not add long explanatory subtitles or dense help copy below a new tool just to describe the feature.
- Put feature instructions, calculation standards, caveats, and FAQs in a separately navigable public guide page or guide modal that follows the existing Card Pone UI.
- A public tool page may link to its guide with a concise `사용 가이드` action. The guide must contain real user-visible content; never add crawler-only, reviewer-only, or hidden SEO text.
- Public guides must use accurate page titles, descriptions, canonical URLs, and structured data that exactly match visible content.

## Production deployment rules

- Production is deployed only from the latest synchronized `main` commit.
- Never run a direct Cloudflare Pages production deployment from a feature or recovery branch.
- Merge current feature changes and automated data updates into `main` before deploying.
- Use `.github/workflows/deploy-production.yml` for every production deployment.
- A data-sync workflow may update `main`, but it must delegate deployment to the production workflow.

- 크레딧 사용량을 최소화해서 작업한다.
- 전체 코드베이스를 스캔하지 않는다. 사용자가 명시적으로 요청한 경우에만 전체 탐색한다.
- 작업 전 관련 파일 후보를 먼저 좁히고, 가능한 최소 파일만 확인한다.
- 관련 파일이 불명확하면 최대 5개까지만 우선 확인하고 요약한다.
- 수정은 최소 diff로 진행한다.
- 불필요한 리팩터링, 포맷팅 변경, 디자인 변경은 하지 않는다.
- 기존 기능과 UI 흐름을 보존한다.
- 새 패키지 설치, 전체 빌드, 전체 테스트는 사용자가 요청한 경우에만 실행한다.
- 삭제, 구조 변경, 환경변수 변경, 인증/DB 관련 변경은 먼저 확인한다.
- 작업 후 수정한 파일과 변경 내용을 짧게 요약한다.

## 커뮤니케이션 규칙

- 사용자의 요청을 실행하기 전에, 이해한 내용을 일목요연하게 되물어 확인한다.
- 확인은 길게 늘리지 말고 핵심 요구사항, 금지사항, 다음 액션만 짧게 정리한다.
- 사용자가 바로 진행하라고 명확히 말한 경우에도, 위험하거나 되돌리기 어려운 작업은 먼저 확인한다.

## 프로젝트 용어 규칙

- 스니덩크의 `A등급`은 Card Pone 홈페이지/API/UI에서 `Single`과 같은 의미로 취급한다.
- 사용자가 `A등급`을 말하면 `Single` 가격, 차트, 최근 거래, 데이터 상태를 확인하는 것으로 이해한다.
- 사용자-facing UI에서는 기존 방향대로 `A등급` 대신 `Single` 표기를 우선한다.

## UI 일관성 규칙

- 새 기능, 새 페이지, 새 모달, 새 컨트롤은 기존 Card Pone 홈페이지의 UI 체계를 최대한 그대로 따른다.
- 기존 버튼, 탭, segmented control, 입력창, 모달, 간격, 타이포그래피, 색상, 테두리, 다크 모드 패턴을 먼저 재사용한다.
- 새 기능만 별도 디자인 시스템처럼 보이거나 기존 화면에서 겉도는 시각 스타일을 만들지 않는다.
- 같은 기능이 카드 도감과 시세 화면 등 여러 위치에 노출되면 공용 컴포넌트를 사용해 동작과 디자인을 동일하게 유지한다.
- 모달, 팝업, 드로어처럼 별도 레이어가 열리면 배경 페이지 스크롤을 잠그고 레이어 내부만 스크롤되게 한다. 레이어가 닫힐 때 기존 스크롤 위치를 보존한 채 잠금을 해제한다.
- 기존 UI와 다른 스타일이 꼭 필요하면 구현 전에 사용자에게 차이와 이유를 설명하고 확인한다.

## PSA10 시세 인덱스 규칙

- 신규 일본판 시세 카드는 SNKRDUNK 최신 카탈로그에서 카드 번호가 확인되는 단품만 자동 편입한다. 박스, 팩, 덱, 세트 상품은 카드 시세와 섞지 않고 박스 탭 수집기로 분리한다. 자동 편입 카드는 D1에 저장해 다음 정기 현재가·거래 이력 수집 대상에 포함한다.

- 홈페이지와 인덱스 API의 대표 인덱스 조건은 `PSA10`만 사용한다. `Single` 인덱스를 다시 노출하거나 대표값으로 바꾸지 않는다.
- 공개 홈페이지에는 `Manga`, `Luffy` 섹터 지수만 노출한다. `Collector`와 `Premium Art`는 공개 대표 지수나 탭으로 사용하지 않고, `Heroines`는 사용자가 구성 카드 목록을 확정할 때까지 숨긴다.
- 인덱스 원천 데이터는 D1 `market_chart_daily_points`의 `source = 'snkrdunk'`, `condition_key = 'psa10'` 일별 거래 중앙값이다. 현재가/최저가만으로 과거 인덱스를 다시 만들지 않는다.
- `2025-01-01`, 최근 1년, 고정 30일 구간을 기준가 절단선으로 사용하지 않는다. 카드별 전체 Trading History에서 최초로 확인되는 안정 가격대를 기준가로 사용한다.
- 같은 날짜의 중복 거래를 제거하고 PSA10 거래가격 전체의 중앙값을 일별 가격으로 계산한다. 기준가격이 없는 첫날이라고 최저 가격군을 자동 선택하지 않는다. 거래가격 편차가 커도 중앙값과 인접 거래일 지지를 먼저 확인한다.
- 카드별 최초 5개 실제 거래일을 기준가 감사 구간으로 확인하되, 기준가는 최초 3개 실제 거래일의 일별 중앙값을 다시 중앙값으로 계산한다. 첫 3거래일 이후의 낮은 가격대를 찾아 기준가를 뒤로 이동시키지 않는다. 이 방식으로 첫날의 고립된 고가·저가 한 건은 중앙값에서 밀어내고, 출시 고가 2거래일 뒤 실제 급락한 경우에는 초기 고가를 보존한다. 달력 날짜가 아니라 실제 거래가 있었던 날짜를 센다.
- 첫날의 고가 또는 저가가 한 건뿐이고 이후 거래일들이 다른 가격대에 모이면 첫날을 이상 거래 후보로 격리한다. 반대로 첫날에 복수 거래가 있거나 다음 거래일도 같은 가격대를 지지하면 실제 출시 가격으로 보존한다.
- 가격 급등·급락은 해당 거래일의 이전 3개와 이후 3개 실제 거래일을 비교한다. 한 날짜만 튄 뒤 기존 가격대로 복귀하면 묶음판매·오매핑·이상 거래로 제외하고, 이후 최소 2개 거래일 이상이 새 가격대를 지지하면 실제 시장 가격 전환으로 인정한다.
- OP16 계열처럼 출시 초기 고가가 여러 거래와 인접 거래일에서 확인된 뒤 지속적으로 하락한 경우 초기 고가와 실제 하락을 모두 보존한다. 결과 지수가 100 아래라는 이유만으로 초기 가격을 낮추거나 제외하지 않는다.
- 거래 수량이 제공되지 않으면 묶음판매 여부를 단정하지 않는다. 2배·3배 등 배수 가격, 단건 거래, 인접 거래일 미지지 같은 근거를 조합해 의심 거래로 격리하고, 판별 신뢰도가 낮은 카드만 수동 검토 대상으로 남긴다.
- SNKRDUNK `trading-histories`를 거래 원천으로 사용하고 `sale-prices` Price Chart는 교차 검증에 사용한다. 두 원천의 초기 또는 최신 가격대가 크게 다르면 한쪽을 임의 채택하거나 D1을 덮어쓰지 말고 해당 카드와 원본 응답을 감사 대상으로 격리한다.
- 모든 섹터 인덱스의 시작일은 구성 카드 중 가장 이른 유효 기준가 확정일이며 그날의 섹터값을 `100`으로 삼는다. `2025-01-01` 같은 공통 고정일을 기준일로 사용하지 않는다.
- 각 구성 카드의 원시 개별 지수는 `해당 날짜의 필터링된 일별 중앙값 / 카드별 기준가 * 100`으로 계산한다. 실제로 지속되는 큰 상승·하락은 임의 상한이나 일간 변화 제한으로 축소하지 않는다.
- 신규 카드는 기준가 검증을 통과한 날 원시 개별 지수 100으로 편입한다. 섹터 지수는 해당 날짜에 기준가가 확정된 모든 구성 카드의 원시 개별 지수를 동일 비중 산술평균한다. 특정 카드, 가격 크기, 거래량에 추가 가중치를 주지 않고 divisor 연결 방식도 사용하지 않는다. 출시 전이거나 기준가가 확정되지 않은 카드는 그 날짜의 평균에서 제외한다.
- 거래가 없는 날은 직전 유효 가격을 유지한다. 미래 거래가격으로 과거 빈 날짜를 보간하거나 이미 확정된 과거 인덱스 경로를 다시 만들지 않는다.
- `1D`, `7D`, `1M`, `1Y`는 API와 차트의 표시 범위일 뿐 인덱스 계산 원천 범위가 아니다. 기간이 지나도 과거 원천 데이터와 누적 계산 기준에서 제거하지 않는다.
- 인덱스는 최신 실제 거래 관측일까지 매일 계산하되, 일별 거래 수집이 완료되지 않은 오늘 날짜를 직전 값으로 미리 생성하지 않는다. `1D`는 최신 완료 일자와 직전 일자의 지수 변동률을 표시한다.
- `.github/workflows/market-history-daily.yml`의 전체 일별 수집이 성공하면 `.github/workflows/market-index-refresh.yml`이 자동 실행되어 PSA10 지수를 최신화해야 한다. 수집과 지수 갱신을 서로 독립된 스케줄로 방치하지 않는다.
- SNKRDUNK Trading History의 ISO 시각은 UTC 문자열을 그대로 잘라 날짜로 쓰지 않고 KST/JST(+09:00) 거래일로 변환한 뒤 일별 집계한다. 이미 날짜만 있는 값은 원래 날짜를 유지한다.
- 수집 페이지가 끝났다는 사실만으로 성공 처리하지 않는다. 원본 거래 수 대비 채택 거래 수, 생성 일별 데이터 수, 최초·최신 거래일, 조건별 거래 수를 검사한다. 키드처럼 수백 건을 수집하고 일별 데이터 1건만 생성된 결과는 실패 또는 격리 처리한다.
- 수집 결과는 후보 데이터로 먼저 생성하고 감사 통과 후에만 기존 D1 데이터를 교체한다. 검증 전에 기존 일별 데이터와 인덱스를 직접 덮어쓰지 않는다.
- 전체 재계산 전에는 반드시 `MARKET_INDEX_DRY_RUN=1`로 다섯 지수와 모든 구성 카드의 기준일, 최초 5거래일 중앙값, 채택 기준가, 최신가, 개별 지수, 제외 사유를 확인한다. 독립 감사에서 최초 기준일 100, 섹터값과 해당 날짜 활성 카드 개별 지수 산술평균의 일치, 원천 간 가격 불일치, 비정상 데이터 축소 중 하나라도 어긋나면 D1 쓰기를 중단한다.
- 인덱스 전체 재계산은 `.github/workflows/market-index-refresh.yml`에서만 수행한다. `market-collector.yml`에 인덱스 전체 재계산을 다시 결합하지 않는다.
- 기준일, 편입 방식, 이상치 한도, 데이터 조건을 변경하거나 D1 전체 재계산을 실행할 때는 사용자 확인 없이 진행하지 않는다.

## 시세 알림 규칙

- 모든 로그인 사용자는 알림 버튼, 규칙 API, Push 구독 API, 정기 평가를 사용할 수 있다. 수동 `테스트 알림` 버튼과 테스트 API만 `user_metadata.username === 'admin'`인 계정에 허용한다.
- ADMIN 알림 모달의 `테스트 알림`은 가격 규칙이나 시세 데이터를 변경하지 않고 현재 ADMIN 사용자의 활성 Push 구독에 확인용 알림 한 건만 전송한다. 일반 사용자 화면에는 이 버튼을 표시하지 않는다.
- 시세 알림은 앱 내부 `user_notifications`와 기기 Web Push를 함께 생성한다. 브라우저 알림 권한과 활성 Push 구독이 없는 사용자는 신규 알림 조건을 등록할 수 없다.
- 알림 권한은 사용자가 알림 기능 안에서 `알림 허용`을 직접 누른 경우에만 요청한다. 권한이 거절된 경우 반복 요청하지 않고 브라우저 사이트 설정 안내를 표시한다.
- iPhone과 iPad에서 Push API를 사용할 수 없는 환경에는 Card Pone을 홈 화면에 추가한 뒤 설치된 웹 앱에서 다시 열도록 안내한다.
- 목표가 알림은 사용자에게 원화로 입력·표시하고 1원 단위의 임의 금액을 허용한다. 내부 수집가가 엔화여도 사용자가 입력한 원화 목표값을 별도로 보존한다.
- 알림 판정에 최신 가격 한 점을 직접 사용하지 않는다. 최근 13시간 내 서로 20% 범위에 있는 최소 2개 수집값의 중앙값을 현재가로 사용한다.
- 최근 기준 중앙값 대비 2배 초과 또는 50% 미만인 급변은 최소 3개 연속 수집값이 같은 가격대에 있을 때만 정상 가격으로 인정한다.
- 24시간 등락률은 정제된 현재 중앙값과 18~36시간 전 스냅샷 중앙값을 비교한다. 유효한 비교값이 없으면 알림을 보내지 않는다.
- 단발성 묶음 판매, 잘못 매핑된 가격, 일시적인 최저가 오류로 판단되는 값은 차트와 알림 판정에서 제외하며, 데이터 신뢰도를 확인할 수 없으면 발송하지 않는 쪽을 우선한다.

## 업데이트 공지 규칙

- 업데이트 공지는 사용자에게 새로 제공되는 기능, 데이터, 서비스 흐름 변경만 기록한다.
- 단순 UI 정리, 간격·색상·반응형·가독성 개선, 성능 미세 조정은 업데이트 공지에 올리지 않는다.
- 기존 공지에 UI 개선 항목만 포함되어 있으면 기능 변경 내용만 남기고 UI 관련 문구는 제거한다.
